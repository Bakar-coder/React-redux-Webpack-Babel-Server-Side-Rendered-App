import React from "react";
import PropTypes from "prop-types";

const RegisterPage = props => {
  return <div className="page register-page">{/* <Register/> */}</div>;
};

RegisterPage.propTypes = {};

export default {
  component: RegisterPage
};
